# Ansible role for installing omero-zarr-pixel-buffer

Default installation in OMERO.server's lib directory.
Adjust `omero_zarr_pixel_buffer_folder` to install for example into omero-ms-image-region microservice.

